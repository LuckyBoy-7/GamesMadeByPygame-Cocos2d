from pyglet.resource import image

class Image:
    background = image("background.png")
    best_score = image("best_score.png")
    current_score = image("current_score.png")
    new_game = image("new_game.png")
